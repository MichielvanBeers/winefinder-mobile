// Native Base
import { Center } from "native-base";

// Components
import LoadingSpinner from "../UI/LoadingSpinner";

const WinesFetching = () => {
  return (
    <Center m="3">
      <LoadingSpinner />
    </Center>
  );
};

export default WinesFetching